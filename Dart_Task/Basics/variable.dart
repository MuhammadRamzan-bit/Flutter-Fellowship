//variable is a container to store value in memory

void main(){
  int a;
  //print(a); //variable must be assign before calling
  a=10;
  print(a);
  double b=2.2;
  print(b);
  //here a and b are variables
}